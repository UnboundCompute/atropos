# Internal-only: import/build scripts, sourcing memos, scratch. Never pushed.
tmp/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Packaging build artifacts. The wheel bundles the catalog under atropos/_bundle at
# build time from the canonical repo-root data; that copy is generated, never committed.
build/
dist/
*.egg-info/
atropos/_bundle/

# OS
.DS_Store

# Claude Code worktrees / local
.claude/

# Internal design notes / roadmap -- never pushed.
private/
